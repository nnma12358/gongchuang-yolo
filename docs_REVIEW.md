# 数据集人工复核 SOP（detection 标签）

> 结论先说：**复核不是"把 150 张图逐张看一遍"，而是"只修该修的、按优先级修、每张一次按键"。**
> 本目录已经把分诊、可视化、复核界面、回写、体检全部脚本化，人工只需在浏览器里做判断。

---

## 1. 为什么必须复核（本项目实测证据，不是泛泛而谈）

| 证据 | 数值 | 说明 |
|---|---|---|
| val 实拍标签来源 | **28/28 全部是几何自动预标注**（`auto-real`） | 唯一的"尺子"本身没有人工确认过 |
| 模型↔标注 IoU | 均值 0.748，**11.6% 的图 <0.5** | 模型和标注"互相不认"的地方，必有一方错 |
| 典型错标 | `real_00029.jpg` IoU **0.07** | 自动框只盖住方块左下角一小块；模型框反而基本正确 → **该图在 val 里会白白扣分** |
| 训练震荡 | mAP 0.85 → 0.2 → 回升 | 标签噪声让模型每轮拟合不同的"错目标"，验证集又是噪声标签 → 指标抖动 |
| 数据泄漏 | **14 对近重复图跨 train/val**（已修） | 同一物体同一姿态的图同时出现在 train 和 val，val 指标虚高 |
| 泄漏修完后的真实水平 | mAP50 **0.9427 → 0.7738** | 去掉泄漏后重新训练（`runs/eval_before_review.json`），说明原来那个 0.94 是虚高的 |

一句话：**尺子是歪的，量什么都是错的。** 先把 val 修准，再谈模型。

> 关于"震荡"：在新切分上重训，mAP50 仍然从 epoch5 的 0.82 掉到 epoch12 的 0.75、
> epoch15 的 0.61（见 `runs/eval_split_before_review.log`）。**标签噪声只是原因之一**，
> 另一个原因是 val 只有 49 张、单张错标就能让 mAP 跳 2~3 个点。
> 所以复核的目标不只是"提高 mAP"，而是**让指标稳定、可解释**。

---

## 2. 复核策略：分诊，不盲看

`tools/label_audit.py` 用两个信号给每张图打"可疑度"（0~100）：

1. **几何规则**：框过小/过大、长宽比 >2.0、严重偏心、贴边
2. **模型一致性**：当前最好模型（`runs/best_v3_gpu.pt`）的预测框与现有标注的 **IoU**；
   模型多检出的框（可能是漏标）、模型完全找不到的图（标注可疑）

实测分诊结果（193 张）：

| 可疑度 | 张数 | 处理建议 | 预计耗时 |
|---|---|---|---|
| ≥40 | **58** | 必看必修 | ~20 min |
| 15~39 | 41 | 顺手扫一眼（拼图 `sheets/`） | ~10 min |
| <15 | 94 | 基本可信，不用逐张看 | — |
| 未标注（`unlabeled`） | 29 | **必须从零画框**（自动预标注失败的图） | ~12 min |

**优先级顺序**（时间不够时按此砍）：

1. **val 里的图**（复核后 val 必须 100% 人工核验）——尺子优先
2. **未标注的 29 张**——目前完全没进训练集，补上直接增加真实数据
3. train 里可疑度 ≥40 的
4. 其余

---

## 3. 完整命令链

```bash
cd /home/xxxffyy/工创/工创yolo

# ① 分诊：算可疑度、出可视化、生成复核包（约 2 分钟，需要 GPU）
/home/xxxffyy/gpu_env/bin/python tools/label_audit.py \
    --weights runs/best_v3_gpu.pt --out data/review --device 0

# ② 先扫拼图（可选，1 分钟抓出"离谱错误"）
#    data/review/sheets/sheet_01.jpg … 每张 20 图，已按可疑度排序
#    绿=现有标注  红=模型(conf≥0.25)  橙=模型低置信

# ③ 开复核台（浏览器，零依赖）
python3 tools/review_app.py --port 8770
#    Windows 侧浏览器打开 http://localhost:8770

# ④ 复核完成后回写源数据集（默认预览，确认后加 --write）
/home/xxxffyy/gpu_env/bin/python tools/apply_review.py --dry-run
/home/xxxffyy/gpu_env/bin/python tools/apply_review.py --write

# ⑤ 重新切分（分组切分：近重复图整体进 train 或 val，杜绝泄漏）
/home/xxxffyy/gpu_env/bin/python tools/build_mix_v3.py --group-split

# ⑥ 体检：格式/几何/缺失/近重复泄漏（泄漏必须为 0）
/home/xxxffyy/gpu_env/bin/python tools/check_labels.py --data data/mix_v3 \
    --csv data/mix_v3/label_check.csv

# ⑦ 重训 + 同口径评估
bash tools/eval_split.sh after_review
#    与前一轮对比：runs/eval_before_review.json vs runs/eval_after_review.json
```

---

## 4. 复核台怎么用（把决策压成一次按键）

`http://localhost:8770`，左侧是**按可疑度排序**的清单（已完成变绿），右侧是图。

| 按键 | 作用 | 使用场景（占多少） |
|---|---|---|
| **A** | 标注正确，下一张 | 约 60%——大部分框是对的，连按 A 即可 |
| **N** | 并入模型建议框（红/橙虚线，**自动去重**，不会越按越多；Shift+N 强制追加） | 约 15%——现有框偏了，模型框更准 |
| **拖拽** | 空白处拖 = 新建框；已有框上拖 = 移动 | 约 20%——模型也不准，手动框 |
| **Z** | 撤销本张的所有修改 | 手滑了 |
| **D / Del** | 删除选中的框 | 框到阴影/背景上了 |
| **1..9** | 选中第 N 个框（Esc 取消选中） | 多目标图 |
| **方向键** | 微调选中框（Shift 大步长，Alt 改尺寸）；**没选中框时 ←/→ 翻页** | 差一点点时比拖拽准 |
| **W** | 跳到下一个未复核 | 中途接续 |
| **, / . 或 G** | 上一张/下一张、跳到指定序号 | 回头检查 |

**自动保存**：任何修改立即写入 `data/review/review_set/labels/*.txt`，断电/刷新都不丢。
`review_state.json` 记录哪些图已确认，可以分多次做。

### 画框标准（保持全库一致，这比"画得特别准"更重要）

- **紧贴目标可见范围**：包含物体全部可见部分（含被遮挡露出的部分），**不含投下的阴影**
- **一个目标一个框**；没有货物就留空（空标签 = 负样本，是有效信息，别乱框）
- 边缘被截断的物体：框到画面边界即可
- 不确定是货物还是杂物：**按货物框**（现场分拣宁可检出也不要漏检）
- 有多个物体：全部框出（自动预标注**每张只给一个框**，这是漏标的主要来源）

---

## 5. 一次复核大概要多久

193 张，按分诊优先级：

| 只做 | 张数 | 预计 |
|---|---|---|
| val（尺子）+ 未标注 | 约 70 | **~25 min** |
| 加 train 可疑 ≥40 | 约 130 | ~45 min |
| 全量 | 193 | ~60 min |

建议**先做前 25 分钟**，立刻重训对比——通常这一步就能看到 val 指标"变低但变可信"。

> ⚠ 预期管理：修完标签后 **mAP 数字可能先下降**。因为原来是在拟合噪声标签，
> 修准后模型反而"不再猜中那些错框"。要看的是：**指标波动变小、训练后段不再震荡**。

---

## 6. 复核完成后的验收标准

```bash
python tools/check_labels.py --data data/mix_v3
```

必须满足：

- [ ] 格式/几何问题 **0**
- [ ] 实拍跨 split 近重复 **0 对**（泄漏）
- [ ] val 中人工核验图占比 **100%**（`subset_summary.json` 的 `groups.val_has_human` ≈ val 实拍总数）
- [ ] 重跑 `label_audit.py` 后，模型↔标注 **IoU 均值上升、<0.5 占比下降**
- [ ] 重训后训练曲线后段**不再出现 0.85→0.2 的塌陷**

---

## 7. 常见坑

| 坑 | 说明 |
|---|---|
| 用 `tools/review_app.py` 时改了 `review_set/labels`，却没跑 `apply_review.py` | 复核结果不会自动进训练集，必须回写 |
| 复核时把"看不清"当成"没有目标" | 空标签会教模型"这里没东西"，错得比画错框更狠 |
| 只复核 train 不复核 val | 尺子还是歪的，重训后指标依然不可信 |
| 用随机切分重训 | 近重复图泄漏会让 val 虚高，必须 `--group-split` |
| 用 Windows 的 `docker.exe` 跑容器挂载 | 挂 `/home/...` 得到空目录（见 sort-web/docker-compose.yml 注释） |
| 改完复核台"看不到框" | 曾有一个**归一化坐标 × 像素缩放比**的 bug：框被画到画布左上角 0.x 像素处，看起来什么都没有。改 UI 后请跑 `python tools/ui_smoke_test.py` 复验（会真的采样边框像素） |
| 反复重建复核包后目录里有旧文件 | `label_audit.py` 现在会先清空 `review_set/` 再写；若手工改过目录请核对文件数 = `audit.json` 的 mapping 条数 |

---

## 8. 脚本清单

| 脚本 | 作用 |
|---|---|
| `tools/label_audit.py` | 分诊打分 + 出 overlay/拼图 + 生成复核包（含模型建议框 `hints.json`） |
| `tools/review_app.py` | 浏览器复核台（零依赖，标准库实现） |
| `tools/ui_smoke_test.py` | 复核台 UI 冒烟测试（真浏览器，防"看不到框/按 N 堆叠"这类回归） |
| `tools/apply_review.py` | 复核结果回写源数据集，溯源标为 `human`，自动备份原标签 |
| `tools/check_labels.py` | 标签体检：格式/几何/重复/缺失/近重复泄漏 |
| `tools/build_mix_v3.py --group-split` | 完全连接聚类分组切分（杜绝泄漏；val 优先放人工核验组） |
| `tools/eval_split.sh` | 当前切分上训练+评估（不部署），给复核前后做同口径对比 |
| `tools/retrain_v3_gpu.sh` / `finish_v3_gpu.sh` | 正式训练→导出量化→评估→部署到 sort-web |

---

## 9. 本轮复核实测结果（193 张做完之后）

```bash
python tools/apply_review.py --write      # 193 张写回，溯源全部 → human
python tools/build_mix_v3.py --group-split # val 40 实拍 100% 人工核验，跨 split 泄漏 0
python tools/check_labels.py --data data/mix_v3   # 格式/几何问题 0，泄漏 0
bash tools/eval_split.sh after_review
```

| 项 | 结果 |
|---|---|
| 复核覆盖 | **193 / 193 张**（164 张有标注 + 29 张待从零标注） |
| 实际改动 | **123 张被修正（75%）**，41 张原标注正确 |
| 中心位移 | 中位 8%，最大 69%（`real_00150` 从画面左下角改到上方中央） |
| 最离谱的原标注 | `real_00053` 的框宽 = **1.0**（横跨整个画面）；`real_00099` 原框在右下角、实际物体在中央 |
| 耗时 | 单人约 1 小时（193 张，按可疑度优先级做） |

**复核对指标的影响**（同一切分协议，只换标签）：

| | 复核前 | 复核后 |
|---|---|---|
| mAP50 | 0.7738 | **0.9927** |
| mAP50-95 | 0.3665 | **0.7158** |
| 模型↔标注 IoU（val 留出集） | 0.758（<0.5 占 10.3%） | **0.853（<0.5 占 0.0%）** |
| 训练曲线 | 0.82→0.61→0.75 反复跳 | **epoch3 起稳定 0.99+** |

**结论**：`docs_REVIEW.md` 第 1 节判断的三个问题全部被证实并解决 ——
val 标签没有人工核验、几何预标注大面积错位、近重复图跨集泄漏。
**"训练震荡"的主因是标签噪声，不是模型或超参。**

### 9.1 复核后仍需注意的

- 新模型在 **38 张图上有背景误检**（实验室设备/纸边被判成货物，val 16 张 / train 22 张）。
  已把部署端置信度阈值从 0.35 提到 **0.45**（实测 R 仍为 1.00、P 从 0.82 → 0.91）。
  现场再收集 30~50 张"货物 + 背景杂物"的照片做负样本微调，可进一步压掉。
- 本数据集只有 3 个"拍摄场次"（文件名时间戳是导出时生成的，相邻帧只差 7ms），
  所以**按时间分组不可用**，只能靠视觉完全连接聚类分组。补数据时请按"场次/光照/批次"分别留出验证集。
