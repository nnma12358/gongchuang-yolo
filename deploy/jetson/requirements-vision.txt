# 视觉容器依赖（Jetson JetPack 4.6.1 / Python 3.6）
# ------------------------------------------------------------------
# 与网关容器同约束（见 requirements.txt 注释）：
#   · pydantic 固定 1.9.2 —— 1.10+ 不支持 py3.6
#   · opencv / numpy / Pillow 走 apt（python3-opencv / python3-numpy / python3-pil）
#   · qrcode 6.1 是纯 Python（py3.6 可用），仅在合成场景生成二维码贴纸时使用，
#     未安装会自动跳过二维码，不影响识别主流程

fastapi==0.68.2
uvicorn==0.15.0
pydantic==1.9.2
starlette==0.14.2
python-multipart==0.0.5
requests==2.27.1
typing-extensions==4.1.1
qrcode==6.1
