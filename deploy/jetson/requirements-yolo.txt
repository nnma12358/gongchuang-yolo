# Jetson YOLO detection container (Python 3.6 / JetPack l4t-base)
# NOTE: numpy + opencv are installed by the Dockerfile, NOT here:
#   - numpy: apt python3-numpy is too old for the opencv wheel -> pip numpy==1.19.5 (py3.6 last)
#   - opencv: bionic apt python3-opencv is 3.2, which CANNOT parse ONNX
#             (cv2.dnn.readNetFromONNX needs >= 3.3) -> pip opencv-python-headless 4.5.4
fastapi==0.68.2
uvicorn==0.15.0
pydantic==1.9.2
starlette==0.14.2
python-multipart==0.0.5
typing-extensions==4.1.1
