# 现场尺度微调 · 首轮候选结果（2026-09-24）

## 数据（首次完整）

| 来源 | 数量 | 说明 |
|---|---|---|
| 原有 mix_v3 | 470 | 含人工复核过的实拍 + 旧合成 + 负样本 |
| **现场几何合成** | 600 | `cam_height=660`，与现场成像尺度一致 |
| **真实标注** | 41 | 现场 Astra 俯拍帧，机器草稿 + 货物区约束（**待人工复核**）|
| **真实背景负样本** | 132 | 从真实帧挖的杂物/地面/空桌面（压假检）|

组装后 **train 1054 / val 189**。

## 训练与导出

- 30 轮（约 9 分钟），基线 `runs/best_v3_gpu.pt`
- **val mAP50 = 0.9611，mAP50-95 = 0.8076**
- 权重归档：`runs/best_overhead_candidate.pt`（`runs/` 未入版本库，在本机保留）
- ONNX：`exports_overhead_candidate_fp32.onnx`
  输入 `[1,3,640,640]` / 输出 `[1,5,8400]` —— **与设备端 `yolo_server` 期望一致，trtexec 重建无需改代码**

## 真实数据实测

| 数据集 | 现役模型 | 候选模型 |
|---|---|---|
| 真实俯拍帧 41 张 | **0 检出**（最高 conf 0.0）| 每帧 **6~8 个**（与草稿标注数量吻合），最高 conf **0.38** |
| 24 张实拍集 | 22/24 | **24/24** |

**结论**：召回已经上去（能找到全部目标、数量与标注吻合），但**置信度校准仍不足**（0.11~0.38，低于部署阈值 0.35）。
原因：真实标注只有 41 张且全是同一姿势，合成数据（600）占比高。

## 下一步（解锁条件）

1. **人工复核 41 张标注**（草稿已在各会话 `labels/`）—— 复核后重训可显著提升置信度
2. **扩充真实姿势**（采集已在等：现场摆货 → 自动记录每个新姿势）
3. 复核 + 扩充后：重训 → 导出 → 设备端 `scripts/build-trt-on-jetson.sh` 重建 FP16 → 用
   `sort-web/scripts/regression_live_sample.py` 做同一套回归对比

**当前设备未部署本候选**：仍运行原 `goods_yolov8n_640_fp16.engine`。
